export default function BatchListModal({ medicine, batches, onClose }) {
  const items = batches.filter((batch) => batch.medicineId === medicine.id).sort((first, second) => first.expiry.localeCompare(second.expiry))
  return <div className="fixed inset-0 z-30 grid place-items-center bg-ink/40 p-4 backdrop-blur-sm"><div role="dialog" aria-modal="true" className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-teal">Inventory detail</p><h2 className="mt-2 font-display text-2xl font-extrabold text-ink">{medicine.name}</h2></div><button onClick={onClose} aria-label="Close" className="text-2xl text-slate hover:text-ink">×</button></div><div className="mt-5 space-y-3">{items.map((batch) => <div key={batch.id} className="flex items-center justify-between rounded-xl bg-mist p-4"><div><p className="text-sm font-bold text-ink">{batch.lot}</p><p className="mt-1 text-xs text-slate">Expires {batch.expiry} · received {batch.received}</p></div><div className="text-right"><p className="font-display text-lg font-extrabold text-ink">{batch.quantity}</p><p className={`text-[10px] font-bold uppercase tracking-wider ${batch.status === 'active' ? 'text-teal' : 'text-coral'}`}>{batch.status}</p></div></div>)}</div></div></div>
}