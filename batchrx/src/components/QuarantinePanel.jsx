export default function QuarantinePanel({ batches, medicines, onResolve }) {
  const quarantined = batches.filter((batch) => batch.status === 'quarantine')
  const findMedicine = (batch) => medicines.find((medicine) => medicine.id === batch.medicineId)
  return <section className="rounded-2xl border border-ink/10 bg-white p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-slate">Audit trail</p><h2 className="mt-1 font-display text-lg font-extrabold">Quarantine zone</h2></div><span className="rounded-full bg-mist px-3 py-1 text-xs font-bold text-slate">{quarantined.length} batches</span></div>{quarantined.length === 0 ? <p className="mt-5 text-sm text-slate">No batches are waiting for a final action.</p> : <div className="mt-4 space-y-3">{quarantined.map((batch) => <div key={batch.id} className="rounded-xl bg-mist p-3"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold text-ink">{findMedicine(batch)?.name ?? 'Unknown medicine'}</p><p className="mt-1 text-xs text-slate">Batch {batch.lot} · {batch.quantity} units · expired {batch.expiry}</p></div><span className="rounded-full bg-coral/10 px-2 py-1 text-[10px] font-bold uppercase text-coral">Quarantine</span></div><div className="mt-3 flex gap-3 text-xs font-bold"><button onClick={() => onResolve(batch.id, 'disposed')} className="text-coral hover:text-ink">Mark disposed</button><button onClick={() => onResolve(batch.id, 'returned')} className="text-teal hover:text-ink">Return to supplier</button></div></div>)}</div>}</section>
}