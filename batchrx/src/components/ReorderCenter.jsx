export default function ReorderCenter({ messages, onNotice }) {
  const pending = messages.filter((message) => message.status === 'pending')
  return <section className="rounded-2xl border border-ink/10 bg-white p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-teal">Action center</p><h2 className="mt-1 font-display text-lg font-extrabold">Reorder signals</h2><p className="mt-1 text-xs text-slate">Generated when stock crosses below its threshold.</p></div><span className="grid h-8 w-8 place-items-center rounded-full bg-teal/10 text-sm font-bold text-teal">{pending.length}</span></div>{pending.length === 0 ? <p className="mt-5 text-sm text-slate">No reorder action is waiting.</p> : <div className="mt-4 space-y-3">{pending.map((message) => <div key={message.id} className="rounded-xl bg-mint p-3"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold text-ink">{message.medicineName}</p><p className="mt-1 text-xs text-slate">{message.sellableQuantity} in date · threshold {message.threshold}</p></div><span className="rounded-full bg-white px-2 py-1 text-[10px] font-bold uppercase text-teal">Reorder</span></div><button onClick={() => onNotice(`Reorder draft ready for ${message.medicineName}.`)} className="mt-3 text-xs font-bold text-teal hover:text-ink">Create reorder draft →</button></div>)}</div>}</section>
}
